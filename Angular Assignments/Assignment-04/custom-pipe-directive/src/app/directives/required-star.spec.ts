import { RequiredStar } from './required-star';

describe('RequiredStar', () => {
  it('should create an instance', () => {
    const directive = new RequiredStar();
    expect(directive).toBeTruthy();
  });
});
